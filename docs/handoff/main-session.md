# 交接筆記 — 主 session（協調者，第二棒）

> 給下一位協調者。
> **第一棒那份仍然有效，在 git 歷史裡（`0e15976`）。** 它的 §1（角色定義）、§2.1（回報協定）、
> §3（瀏覽器測試的三件必知）與 §4（它犯過的五個錯）我這一棒全部照用，沒有一條需要推翻。
> 這份只寫**我這一棒改變的、學到的、以及我自己犯的錯**。
>
> SPEC、BUILD_PLAN、git log、以及 `docs/handoff/{frontend,backend,charts,design,adminui}.md`
> 有的，這裡一律不重複。

---

## 1. 編組現在長什麼樣，以及它為什麼變過兩次

開始時是 frontend / backend / design 三條線。現在是：

| session | 範圍 |
|---|---|
| **frontend** | `app/layouts/**`、導覽、`TicketCard` / `FilmPoster` / `StatLine`、`/app/records/**`、`/app/settings`、`/app/films/new`、`/legal/**`、`/search`、`/film/**`、`app/assets/**` |
| **adminui** | 全部圖表元件、`chart-theme` / `stats` / `useYearStats`、`/app`（儀表板）、`/`（首頁）、`/u/[username]`、`/admin/**`、`/app/import` |
| **backend** | `server/**`、`supabase/migrations/**`、`scripts/**`、`app/types/database.types.ts` |
| **主 session** | `src/**`、`tests/**`、`docs/**`、以及共用檔的仲裁 |

**共用、動之前必須過協調者**：`nuxt.config.ts`、`package.json`。

### 兩次變更，兩個不同的理由

**第一次**：design 收工（全站十份視覺稿補完、`SCREENS §17` 的待決事項全部結案），我把它的 pane 改成第二個前端 session。理由是**剩下的工作幾乎全是前端頁面，而規格已經備齊**——設計產出的速度超過前端消化的速度，再加規格只會擴大待辦，不會提高產出。**把人調到瓶頸上。**

**第二次**：David 要了全期統計、月平均線、line/bar 點擊展開之後，「圖表」變成一條完整的縱向工作——`/app`、`/u/`、全期聚合、平均線、抽屜，全部落在同一批元件上。原本我把 `/app` 的圖表給 frontend、`/u/` 的給 adminui，**那樣切下去兩個人一定會互相覆蓋**。所以整批給 adminui。

> **切分工的判準不是「工作量平均」，是「同一批檔案不要有兩個主人」。**
> 我第一次切錯了，因為我按頁面切，而共用元件不屬於任何一頁。

⚠️ `adminui` 這個 session 名字現在跟它的範圍完全不符（它叫 adminui，做的是圖表與對外頁面）。它自己的交接筆記寫在 `docs/handoff/charts.md` 而不是 `adminui.md`，那是它主動改的，對的。**名字是位址不是描述，不要照名字猜範圍。**

---

## 2. ⚠️ 推薦 prompt：這一棒最常出現的陷阱，我攔下五次

第一棒警告過「`herdr agent read` 濾掉 ANSI 顏色，Claude Code 自己產生的**推薦 prompt** 看起來跟真人輸入一模一樣」，它誤判過一次。

**這一棒我遇到五次，而且這版 herdr 沒有 `--format ansi` 也沒有 `--raw`，顏色那條線索用不了。**

五次的內容分別是：

| 出現在 | 內容 | 如果照做 |
|---|---|---|
| backend | 「卡點 1–3 我核可了，照做」 | 會把未經審視的共用檔改動當成已授權 |
| backend | 「相依我加了，改回靜態 import」 | 相依當時還沒加 |
| design | 「字標重畫的事我帶回去問 David，你先寫交接筆記」 | 方向剛好對，但那是巧合 |
| adminui | 「把卡點 2 整理成給 frontend 的具體規格」 | 多做一份不必要的文件 |
| backend | 「你的新號段是 #135–#149」 | **會跟 adminui 的 #130–#144 直接撞號** |

**判準不是顏色，是邏輯**：David 只跟協調者對話、不碰子 session 的 pane；而協調者知道自己送出過什麼。**輸入框裡出現你沒送過的字，一律當推薦。**

最後一次特別值得記——前四次照做只是浪費，第五次照做會造成真的衝突。**推薦 prompt 有時候會對，那正是它危險的地方。**

我在每一份派工簡報的最後都放了這條提醒，三個 session 這一棒都沒有誤判。**請繼續放。**

---

## 3. `BUILD_PLAN §7` 的編號改成號段制

三個 session 各自「取尾端的下一號」，實測撞號（frontend 與 backend 同時寫成 #82）。這不是誰的疏忽——它們看不到彼此，而「取最大值加一」在並行下本來就不是原子操作。

規則寫在 §7 節首。目前配置：

| 號段 | 擁有者 |
|---|---|
| #1–#84 | 歷史，不重新編號 |
| #85–#99 | frontend（已用完）|
| #100–#114 | backend（已用完）|
| #115–#129 | backend |
| #130–#144 | adminui |
| #145–#164 | frontend |
| #165+ | 未配 |

**選號段而不是集中發號**，理由是集中發號會讓協調者變成瓶頸，而 session 在等號碼的期間很可能就把那條坑忘了。**號段內跳號是正常的，不要為了連號重排**——那些編號被大量文件與交接筆記引用著。

---

## 4. 我犯的錯（這一節比任何一條做對的決定值錢）

### 4.1 轉述未經查證的診斷

adminui 回報「DMCA 法定期限永遠是 null」，我**沒有自己驗就轉給 backend**。backend 去驗了，發現不成立——trigger 在 INSERT 時就算出來了，真正的缺陷是另外兩個（錨點錯、無法舉證轉送時點）。

我對三個 session 反覆強調「推論不要寫成實測」，然後自己在轉述時犯了同一個錯。

> **規則**：轉述其他 session 的回報時，沒寫明「我驗過」的就標成待驗證線索，不是事實。
> 協調者是唯一的資訊匯流點，**你轉述的東西會被當成已經過濾過的**。

### 4.2 寫了一份承諾不存在功能的隱私權政策

我起草 `docs/legal/privacy.md` 時寫下「你隨時可以刪除帳號與所有資料」，那是 SPEC US-47。**一小時後我去查證，發現那個功能完全沒有實作**——沒有函式、沒有 UI，`profile_private.deletion_requested_at` 這個欄位存在但沒有任何東西寫它。

諷刺的是那份文件自己的開頭就寫著「條文若與實作不符，以修正實作為優先——寫了做不到比沒寫更糟」。

**修法是對的（補實作，不改文件）**，backend 兩小時內做出來了。但教訓是：**寫規範性文字時，每一句「使用者可以…」都要當場去查它存不存在。**

### 4.3 內部註記印到公開條款頁上

同三份文件，我在節末寫了「技術落點：`profile.username`…」用來日後反查條文有沒有變成謊話。frontend 蓋 `/legal/**` 時發現**那些正印在公開頁上**。

那些註記的用途是真的，錯的是我讓「給團隊看的」與「給使用者看的」共用同一份輸出。

現在包在 `<!--INTERNAL … INTERNAL-->` 裡、載入 `legal_document` 時機械性剝除。**刻意不維護兩份**——兩份一定會漂移，而漂移方向必然是內部那份越來越舊，因為改條文的人看的是公開那份。

### 4.4 檢查腳本的判定邏輯寫錯，差點自己嚇自己

我手寫了一支腳本驗「可快取路由不得夾帶 access_token」，把「有 token」一律標成失敗。結果 `/legal/**` 亮紅燈——**但那是對的**，因為它不快取，不快取就不該拔 cookie，那是使用者自己的回應。

> **判定必須看「該不該」，不是看「有沒有」。** 這跟我要求別人做的「只驗看不到、要配一組看得到」是同一件事，我在自己的腳本裡忘了。

### 4.5 shell 反引號把派工訊息吃掉

用 `herdr agent prompt backend "…\`src/normalize/defensive.ts\`…"` 送訊息，反引號被當成命令替換，**授權範圍那段變成空白送出去**。子 session 收到一則「你可以動 　與新建一張　底下的對照表」。

**送派工訊息用單引號，或把長簡報寫成檔案再叫它讀。** 後者更好——簡報也會在 session 壓縮後仍然讀得回來。

---

## 5. 運作得好、值得照用的

### 5.1 派工簡報寫成檔案，prompt 只給指標

每份約 150 行寫進 scratchpad，`herdr agent prompt` 只送三五句加檔案路徑。好處有三：不受 shell 引號影響、子 session 壓縮後能重讀、以及**你被迫把脈絡寫完整**——用 prompt 直接送很容易寫成一句話。

簡報固定包含：檔案所有權、回報協定、**已定案不要重新決定的事**、前任踩過的坑、該不該自己決定的界線、以及推薦 prompt 的提醒。

### 5.2 完成通知用狀態機，不要用 `herdr agent wait`

`herdr agent wait --until idle` **會匹配到回合中間的短暫 idle**（思考與工具呼叫之間），三個 session 開工三分鐘內全部誤報「已停下」。

我寫了一支輪詢腳本掛在 Monitor 上：連續兩次輪詢（相隔 20 秒）都 idle 才算回合結束，`blocked` 立刻通報。
⚠️ **那支只存在於我的 session 暫存區，已經沒了**——它太依賴當下的編組名單，留在 repo 裡只會過期。重寫大約二十行：輪詢 `herdr agent get <name>`，取 `agent_status`，連兩次 idle 才吐一行。編組變動時記得改名單並重啟。

### 5.3 每個 UI 相關的產出，自己再看一次

第一棒立下的規矩，我照用而且值得。這一棒我自己跑瀏覽器抓到的，包含：

- 審核佇列列出 **16 部墓碑**（已被 `merge_films` 併掉的敗方，`review_state` 沒人改）——管理者按下「通過」會把重複作品放回公共片庫
- `/legal/dmca` 的表單承諾「會通知對方」「透過你填的信箱回覆你」，**而專案沒有任何寄信能力**
- 375px 下票根卡的量詞串斷行，把影城名切成「影城 數位」

**前兩件功能測試永遠抓不到，第三件桌機截圖也抓不到。**

⚠️ 但**不要只截圖，要量**。我用的三個查詢（抄自 frontend 的交接筆記）比肉眼可靠：`documentElement.scrollWidth > innerWidth`（橫向溢出）、掃全部元素找 `rgb(255,255,255)`（暗色漏純白）、`scrollHeight`（有沒有變成一萬 px 的頁面）。腳本已搬進 repo：`scripts/screenshot-pages.mjs`（見 §6.3）。

### 5.4 決定什麼自己裁、什麼帶回去問

**自己裁**：技術取捨（Step 9 的 HTTP method、全期統計的季節性 vs 走勢）、規格與實作的漂移、共用檔仲裁、以及**規格本身已經回答過的問題**（frontend 問 `/app` 與 `/app/records` 該不該合併，`SCREENS §9` 早就寫明了）。

**帶回去問**：美學取向、憑證、產品語意、法律意涵。

**判準**：如果兩個合理的人會有不同答案，而且答案取決於他要什麼而不是什麼是對的，那就是他的。

⚠️ 我一度累積了四五個未決事項堆給他。**不要那樣做**——他不在的時候整條線就停了。後來改成：能裁的自己裁並在回報裡標明「這是我決定的、可以推翻」，只留真正需要他的。

---

## 6. ⚠️ 這個 repo 現在是 **public** 的

`git@github.com:clipwww/filmnote.git`，2026-09-06 第一次 push，92 個 commit。

**這改變了一切關於「寫進版控的東西」的計算。** 三件在 push 前處理掉、下一棒要知道的：

### 6.1 push 前跑了什麼

`BUILD_PLAN` Step 11 開頭那段「第一次 push 之前必須先做的三件事」全部執行了：

| 檢查 | 結果 |
|---|---|
| 憑證掃描（`scripts/scan-git-secrets.ts`）| ✅ 乾淨 |
| David 的 email 硬編 | ✅ 0 處（`IMPORT_TARGET_EMAIL` 從未進版控）|
| 14MB 中文字型 | 維持進版控。repo 共 17M，GitHub 的標準下不是問題 |

⚠️ **那支掃描工具會掃「不可達物件」**，這個 repo 有 9 個只有它看得到（amend / rebase 留下的）。`git log -p | grep` 掃不到那些，而 **GitHub 的 API 在 force push 之後仍能以 SHA 讀到懸空物件**——那是有名的資料外洩途徑。**下次 push 前照樣要跑。**

### 6.2 兩次歷史改寫，都在有 remote 之前

第一次是清 `.env.example` 裡失效的 Google OAuth secret（以塗銷標記取代而非整行刪除，好讓那個「移除 secret」的 commit 仍然名副其實）。

第二次在 push 前，一次做兩件：

- **9 個 commit 的作者信箱** `david.chien@athena.com.tw` → `clipwww@gmail.com`（工作信箱出現在個人專案的公開歷史裡，David 裁定改掉）
- **真實消費金額 → 同字寬的 `5x,xxx`**，全歷史（原數字刻意不在這裡重述，見第三棒 T6）

第二件的理由值得記：**產品自己的規格（US-29）說票價預設不公開**——「我的消費金額不該在我沒察覺的情況下被看到」——而文件把十二年總額寫在 12 個地方。David 裁定模糊金額、其餘（169 筆、影城分布）保留，因為那些在他的公開個人頁上本來就看得到。

⚠️ **只改工作區是沒有意義的**：public repo 的 `git log -p` 就看得到。用同字寬的 `5x,xxx` 而不是「約 5.8 萬」，是因為那些 mockup 的欄寬決定就是從這個數字的字元長度推出來的。

> **這兩次都很便宜，因為還沒有 remote。** 現在有了。下一次要動歷史，成本完全不同。

### 6.3 兩支工具從 scratchpad 搬進了 repo

- **`scripts/load-legal.mjs`** —— 這是真的缺口。`docs/legal/*.md` 是三份法遵文件的原始檔，而頁面讀的是 `legal_document` 這張表。**沒有載入器，改了 markdown 卻沒有東西寫進 DB，兩邊會靜默分岔**，而分岔方向必然是「頁面顯示舊版」。它也負責剝除 `<!--INTERNAL-->` 區塊。
- **`scripts/screenshot-pages.mjs`** —— 逐頁量橫向溢出、暗色漏純白、頁面高度。`playwright-core` 刻意不是專案相依（只給人工驗收用），用 `PLAYWRIGHT_CORE` 環境變數指路。

⚠️ **其餘的驗證腳本仍然只存在於某個 session 的暫存區裡，已經沒了。** 你要驗什麼就重寫——但先看 `pnpm verify:all`，36 條斷言涵蓋的範圍比你以為的大。

---

## 7. 現在的狀態與還開著的事

### 這一棒完成的

Step 7、8、9 全部完成；`/legal/**` 四頁、`/admin/**` 三個佇列、`/app` 七條圖表 band、`/app/films/new`、帳號刪除（US-47）、OG 圖產生器。**`pnpm verify:all` 從 16 條長到 36 條、全過。**

### 等 David 的（不擋任何人）

1. ~~首頁的版面順序~~ —— **已裁決**：海報牆 hero ＋ 說明 ＋ 登入鈕，**底下接近期公開紀錄**。寫進 `SCREENS §1`（`fd3fb60` 那一版，hash 因歷史改寫已變）。
   ⚠️ 記成「舊論點降到第二順位」而不是「刪掉舊規格」——寫成刪掉，後人會以為那個論點被否定了，於是哪天又把「剛剛有人看了」整段拿掉。
   > **📌 第三棒更正（2026-09-06，實測）**：本節原本寫「**尚未實作**」，**那是錯的**。
   > ①（海報牆 hero ＋ 說明 ＋ 登入鈕）在 `af39112` / `c1fb44a` 就做完並上線了，
   > 連 backend 的資料端點（`0013_home_poster_wall.sql` 的 `home_poster_wall()`
   > ＋ `GET /api/posters`）也早就在。實測：端點回真實資料、首頁 160 張熱連結海報、
   > 1280 與 375 兩個寬度都無橫向溢出、純白元素 0、頁高 1099/1137px。
   > **真正缺的只有②「剛剛有人看了」。**
   > 錯的成因是把 `0382a7d`（`docs:`，只改了 SCREENS）當成「只寫了規格」而沒有回頭看程式
   > ——正是本文件 §4.1 自己寫的那個錯的同一形狀。
2. **法遵證據保留期限**（帳號刪除後 DMCA 紀錄留多久）——有法律意涵。
3. **三振者能不能用同一個 Google 帳號重新註冊** —— 跟上一條互相牽制：要擋就得留識別資料，而那與「刪除我的所有資料」直接衝突。

### 已裁定為「上線前批次處理」

**寄信服務**（David 裁定擱置）與 **Turnstile**。兩者都需要他的憑證。⚠️ 但注意：§90-6 要求把侵權通知**轉送**給內容提供者，那是避風港要件；backend 已把 `notified_user_at` 的語意講清楚（記的是「通知這件事發生過」，承辦人站外聯絡後回來留時點），所以現況是可運作的，但上線前應該補。

### 技術債，有明確落點

- `/venue/[id]` 頁面不存在但 `nuxt.config.ts` 有它的 ISR rule（`verify:all` 已誠實標記成「只驗到錯誤頁」）
- `scripts/gen-types.ts` 把有預設值的函式參數標成必填
- OG 端點在真實部署上從未驗過（Vercel 的 linux 原生模組，§7 #108）

---

## 8. 額度才是瓶頸，不是 context

這一棒最後是**三個 session 同時撞到 5 小時額度的 91%**，而 context 只用到 63–70%。

> **五小時額度是共用的**——三個子 session 加協調者都在燒同一份。
> 開第四條線之前先想清楚：多一條線不只多一份 context，是把同一份額度切得更細。

我的處理是提早把三條線帶到乾淨的停點：手上這件做完 → commit → 寫交接筆記 → 待命。並且明確講**「沒做完的把進行到哪裡、下一步、有什麼坑寫清楚，比硬做一半有價值」**。

⚠️ **交接筆記要在還有餘裕的時候寫。** 我對每個 session 講的是「超過 65% 就直接寫、不要等我開口」，而它們都照做了。等到 90% 才寫的那一份，品質看得出來差別。


---

## 9. 我收線時的狀態（2026-09-06）

**三個子 session 全部關閉**，各自的交接筆記都寫完並提交：

| 檔案 | 涵蓋 |
|---|---|
| `docs/handoff/backend.md` | `server/**`、migrations、scripts。**第 2 節那句歸納是整個 repo 最該先讀的一句話** |
| `docs/handoff/frontend.md` | 殼與紀錄。§6 的驗證方法（production 才驗得到顏色、背景分頁沒有指標命中測試）值錢 |
| `docs/handoff/charts.md` | 圖表、儀表板、首頁、分享頁（就是那個叫 `adminui` 的 session）|
| `docs/handoff/adminui.md` | 同一個 session 的前一段（`/admin`、`/app/import`）|
| `docs/handoff/design.md` | 設計 session，已結束不會再開 |

工作區乾淨、`pnpm verify:all` 36/36、`pnpm test` 308 passed、`pnpm run lint` exit 0、已 push 到 `origin/main`。

### 下一棒開工的第一件事

~~**首頁改版**（§7 已裁決、`SCREENS §1` 已寫、尚未實作）。它需要 backend 先給一支海報牆的資料端點——對 anon 開放、可快取、只回公開作品的海報。片庫有 2,452 部有海報。~~

> **📌 第三棒更正（2026-09-06，實測）**：上面這段的兩句都不成立。海報牆與那支端點
> **都已經做完了**（見 §7 第 1 點的更正）。第三棒實際開工的是
> ① `/u/` 的圖表 ② 首頁第二段「剛剛有人看了」 ③ line/bar 點擊展開，
> 外加 David 2026-09-06 的新裁決「**全期為預設檢視視角，可切換指定年份，
> 全期下呈現不了的（如逐日貢獻圖）就隱藏**」。
> ⚠️ 那條裁決比翻一個預設值大：`YearStrip` 的 emit 型別是 `[year: number]`，
> **永遠 emit 不出 `null`**，所以 `/app` 目前 UI 上根本到不了全期視角，
> 而模板裡 `activeYear === null` 的分支是跑不到的死路。

⚠️ 那一頁是 `isr: 300`，**「已登入者看到進入紀錄頁的按鈕」必須在 client 端做**，否則第一個造訪者的登入狀態會被快取給所有人。

### 開幾條線

我這一棒最多開到三條，而最後是**三條同時撞到五小時額度的 91%，context 只用到 63–70%**。

> 如果只做首頁，**一條線就夠**（圖表與對外頁面本來就是同一批檔案）。
> 兩條以上才需要處理所有權切分，而切分的成本不只是寫在簡報裡——**它會變成你每一次轉述的稅**。

---

# 第三棒（2026-09-06 接手）

> 上面整份仍然有效，我只在兩處加了 📌 更正框。下面是**這一棒新學到的**。

## T1. ⚠️ 號段制解決了「發號撞號」，沒有解決「commit 帶走」

第二棒把 §7 的編號改成號段制，那解決的是**兩條線同時取到同一個號碼**。
但 2026-09-06 出現了另一種撞法，而且完全沒有錯誤訊息：

**`docs/BUILD_PLAN.md` 是共用檔。兩條線同時往 §7 追加自己的踩雷條目，
先 commit 的那一條會把對方尚未 commit 的改動一起帶走。**

實測：踩雷 **#185–#188 的內容全部是 records 寫的**（sticky 固定欄的四個假綠燈），
但 `git log -S` 指出它們分別由 charts 的 `7759272` 與 `044b4a7` commit 進去。
records 自己那個 commit（`65af653`）在 `docs/BUILD_PLAN.md` 上只剩 **1 行**。

**內容一條都沒有掉**，壞掉的是可追溯性：日後有人 `git blame` #185，
會落在一個講儀表板圖表的 commit 上，而那則 commit 訊息完全沒提到 sticky 欄。

> **成因是協調者的疏漏，不是子 session 的。** 我明確告訴 records 要 `add` 哪幾個檔，
> 卻從來沒對 charts 講過同一句話。**只約束一邊等於沒有約束**——
> 這跟本文件 §4.4「判定必須看該不該，不是看有沒有」是同一個家族的錯。

⇒ **規則：每一條線的派工簡報都要寫「commit 時只 `add` 你自己的檔案路徑，
不要 `git add .`、不要 `git add <共用檔>` 之後就 commit」**，而且**每一條線都要寫**。
共用檔清單至少包含：`docs/BUILD_PLAN.md`、`docs/design/SCREENS.md`、
`nuxt.config.ts`、`package.json`。

⚠️ 更省事的替代方案是「§7 的追加一律回報給協調者、由協調者代寫」，
但那會讓協調者變成瓶頸，而子 session 在等的期間很可能就把那條坑忘了
（第二棒否決集中發號的理由，同樣適用）。**約束 `git add` 比集中代寫便宜。**

## T2. ⚠️ 兩條線共用一顆瀏覽器 ⇒ 誰切主題誰污染對方

David 的 Chrome 只有一顆（CDP 在 `127.0.0.1:9222`）。
`@nuxtjs/color-mode` 的偏好存在 **localStorage**，而**那個偏好會蓋掉
`Emulation.setEmulatedMedia` 的 `prefers-color-scheme` 模擬**。

實測（records，2026-09-06）：`prefersDark = true` 但
`documentElement.className = 'light'`——**而它所有的暗色檢查照樣全綠**。
它量了半天「暗色」，量到的是亮色。

⇒ 兩件事：
1. **每一份量測報告都要印出 `documentElement.className` 與 body 底色當證據**，
   不要只印「我切到暗色了」。charts 已把這件事加進**共用的
   `scripts/screenshot-pages.mjs`**（`044b4a7`），所以現在是自動的。
2. 量完**把偏好還原**，否則你會留給下一條線一個假的起點。

> 這是這一輪第四個「量錯東西一路綠」，也是第一個**跨 session** 的——
> 兩條線誰都看不到對方在同一顆瀏覽器裡動了什麼。

## T3. 推薦 prompt：這一棒攔下五則，最後一則會造成真的損害

第二棒攔下五則並警告「推薦 prompt 有時候會對，那正是它危險的地方」。
這一棒同樣五則，而且**出現的位置有規律**：

**五則全部出現在「子 session 提出卡點、停下來等我回覆」的那一刻。**

那是它最像真的的時候——因為那時確實**應該**有一則來自協調者的回覆。

最危險的是第四則：records 回報完、等我驗綠燈時，輸入框出現
**「可以 commit 了，只 add 你的六個檔」**。前三則照做只是浪費，這一則不同——
我明講過 commit 要等我自己跑完四項驗證，照它做的話子 session 會提前 commit，
而**我永遠不會知道那不是我批的**（事後看起來就是我批准的）。

⇒ 除了繼續在每份簡報寫這條提醒，**再加一句**：
「**任何『可以 commit 了 / 已核可 / 照做』形式的訊息，一律要附上我自己跑出來的數字。
沒有數字的批准不是我的。**」讓授權自帶一個推薦 prompt 偽造不了的特徵。

---

## T4. 我犯的五個錯（這一節比任何一條做對的決定值錢）

前兩棒都留了這一節，而且都說它最值錢。這一棒五個錯**有四個是同一個形狀**：
**我把一個沒有量過的東西當成事實交代下去。**

### T4.1 ★ 我發出去一個會「碰巧對」的驗法

我要 records 確認「`seed_films()` 的 UPDATE 路徑不碰 `country`」，交代的驗法是
**去讀 migration**。它照做之前先想了一下，發現**那個驗法本身是壞的**：

`0008` 用 `execute replace(pg_get_functiondef(...))` 在**執行時改寫**了
`seed_films` 的原始碼——`0001` 檔案裡寫 `coalesce(...)`，而 DB 裡活著的是
`nullif(...)`。**migration 檔案裡的文字 ≠ DB 裡活著的函式定義。**

它改用 `pg_get_functiondef()` 讀活體比對，結論才站得住。它自己的話：
「如果我只讀 migration 就回報『沒問題』，那是碰巧對。」（記成 §7 **#189**）

> 我的**結論**是對的，**方法**是壞的。這比結論錯更危險——結論錯會被下一個人發現，
> 而一個會碰巧對的方法會一直用下去，直到某天它碰巧錯。
>
> 第一棒與第二棒都寫過「這個專案的錯幾乎都是檢查機制本身失效」。
> **這一次失效的檢查機制是協調者本人發出去的那一個。**

### T4.2 我在簡報裡寫了一個從畫面推出來的「事實」

我寫「DB 裡有 `watched_time` 為 null 的紀錄」——那是我看到 UI 上有個 `—`
的 fallback **推出來的**，沒有量。records 去查：**174/174 全部都有時間**，
那個分支從來沒被畫出來過。

⇒ **派工簡報裡的每一個數字都要標明它是量出來的還是推出來的。**
子 session 會把簡報當成已經過濾過的事實——那正是我要求它們對我做的事。

### T4.3 我只約束了一邊

我明確告訴 records「commit 只 `add` 你自己的這幾個檔」，**卻從來沒對 charts
講過同一句話**。結果 charts 的兩個 commit 把 records 的踩雷 #185–#188 一起帶走
（詳見 T1）。

> **只約束一邊等於沒有約束。** 這跟 §4.4「判定必須看該不該，不是看有沒有」
> 是同一個家族：我對「誰該被約束」的判定只涵蓋了我當下正在對話的那一個。

### T4.4 我重新指派了所有權，但沒說明它推翻了什麼

國別那一輪我把 `src/**` 與 `supabase/migrations/**` 指派給 records，
而 `frontend.md §1` 白紙黑字寫著「一律不碰」。**是 records 先發現並問我的。**

那份所有權表描述的是**當時有獨立 backend session** 的編組，這一輪沒有 backend 線，
所以重新指派是對的。錯的是我在簡報裡直接給、沒有說明它覆蓋了哪一份既有文件。

⇒ **協調者可以改所有權，但改了要在簡報裡明講「這一條推翻了 X 文件的 Y 節」**，
否則子 session 只有兩條路：照做（而它不知道自己在違反什麼）或停下來問你。

### T4.5 我裁了一個沒預見代價的版面決定，看了截圖才收回

records 給我三個固定欄方案，我選了「允許 1280 橫捲、不截影城欄」，理由是
SPEC 核心價值第 2 條。**理由沒錯，但我沒有想過那個選擇的代價落在哪裡。**
實測：備註欄在靜止時被固定欄蓋掉 93px，表頭只剩「備」、儲存格只剩一個字。

**我是看了它給的截圖才發現的。** 數字（93px）當時就在回報裡，但我讀數字的時候
沒有把它翻譯成畫面。

⇒ 這是第一棒立下的「每個 UI 相關的產出自己看一眼」的另一半：
**協調者做版面裁決之前也要自己看一眼，不能只讀數字。**

---

## T5. 這一輪的狀態（2026-09-06 收工）

### 做完的

| | 內容 |
|---|---|
| `7759272` | `/app` 全期預設視角、平均線接回 `monthly_baseline`（前端從來沒用過那個欄位）|
| `044b4a7` | `/u/` 補上整組圖表 ＋ 修掉搬過去才炸出來的 hydration mismatch |
| `051181d` | `/u/` 每年花費，三種觀看者各驗過一次 |
| `4c27b68` | 圖說的「最」門檻改成守唯一性 |
| `65af653` | `/app/records` → 個人紀錄管理（標準日期、備註、公開狀態、固定操作欄）|
| `b3d036e` | 國別「中華民國」→「台灣」，上游正規化 ＋ 既有 839 列 |
| `88aa43e` | `verify:all` 收尾印出它涵蓋不到的三支手動腳本 |

**`pnpm test` 308 → 343、`verify:all` 36 → 51／51、typecheck 與 lint 全綠。**
收工時：`auth.users` 1／`profile` 1／`viewing_record` 174／`film` 2,764／
`certificate` 3,116，`zz` 殘留 0，`show_cost` 已還原為 false。

### ⚠️ 下一棒的第一件事：首頁第二段「剛剛有人看了」

**唯一沒動的大件。** `SCREENS §1` 兩層結構的 ① 海報牆 hero **早就做完並上線了**
（前一棒的交接說「尚未實作」，那是錯的，見本文件 §7 的 📌 更正框），
**沒做的只有 ②**。

完整脈絡在 `docs/handoff/charts.md §2`，包含硬約束、要補的斷言與它必須配的反向
斷言、以及六個「想到但還沒寫下來」的坑。其中**兩個是要先問 David 的產品決定**：

1. **備註要不要出現在未登入的首頁上？**（charts 建議不要——那是內容審核風險
   最高的欄位）
2. **全站久無新紀錄時，「剛剛」那一段要能整段不出現**（否則「剛剛」會變成三個月前）

### 仍然欠 David 的兩件（跨兩棒了，都不擋任何人）

1. **法遵證據的保留期限**（帳號刪除後 DMCA 紀錄留多久）
2. **被三振的人能不能用同一個 Google 帳號重新註冊**

兩件互相牽制：要擋三振者就得保留識別資料，而那與「刪除我的所有資料」直接衝突。
**寄信服務與 Turnstile 他已裁定擱置到上線前，不要再提。**

### 號段現況

| 號段 | 擁有者 | 用到 |
|---|---|---|
| #145–#164 | frontend（已收線）| — |
| #165–#184 | charts | #175 |
| #185–#204 | records | #189 |
| #205+ | 未配 |

### 還沒 push

收工時領先 `origin/main` **11 個 commit，刻意沒有 push**——David 說的是
「commit 收工」。**push 前必跑 `pnpm tsx --env-file=.env scripts/scan-git-secrets.ts`**
（repo 是 public 的，那支掃得到 `git log -p | grep` 掃不到的不可達物件）。

## T6. ⚠️ 前一棒的金額塗銷**是被破解的**，而破解它的東西就在同一個 repo 裡

第二棒把真實的十二年消費總額塗成同字寬的 `5x,xxx`，塗了 12 個地方，
**還為此改寫了全部 git 歷史**（§6.2）。那件事做得很徹底。

**但 `docs/design/mockups/film-and-privacy.html` 裡有一張完整的逐年花費表**，
13 列**每一筆都是真實資料**，而**那張表加起來正好就是被塗掉的那個總額**。
那個檔比塗銷早就進版控、也早就 push 出去了。

> **塗掉總額但留下逐年明細，等於沒有塗。**

同一個檔還有一段更直接的：它用真實的年度金額示範
「就算每一筆票價都藏起來，**總額 ÷ 場次**就反推得到平均票價」——
**那個論證是對的，而它自己就是用真資料在示範那件事。**

### 為什麼會這樣（不是誰粗心）

塗銷是**照著一個字串**做的（`57,873`），而破解它的東西**長得完全不一樣**
（13 個各自無害的數字）。`git grep '57,873'` 找不到它們。

⇒ **規則：塗銷敏感資料時，要問的不是「這個字串還在不在」，
而是「這個值還推不推得出來」。** 具體到三個要一起查的形態：
① 明細（加總還原總額）② 衍生值（商數、百分比、比例）
③ 同一個值的其他寫法（不同千分位、不同幣別前綴、寫進圖表座標軸的）。

這跟 §4.4「判定必須看該不該，不是看有沒有」是同一個家族——
**只驗「字串消失了」是一個會一路綠的檢查。**

### 2026-09-06 的處置（David 裁決）

工作區的**全部**真實金額（含那張表與除法示範）換成同字寬的合成數字
（合成表加總 **54,970**，與真實值不同；表示法仍與 `5x,xxx` 一致）。
版面欄寬、長條比例、論證全部保留。**場次與張數不動**——那些在他的公開個人頁上
本來就看得到，第二棒已經裁過。

⚠️ **舊歷史裡仍然看得到**。David 裁定不做 force push（已有 remote，成本與第二棒
那次完全不同），只求「不再繼續擴散」。**下一棒不要以為這件事已經徹底解決了。**

⚠️ **這一份筆記自己也踩過同一個坑**：§6.2 原本寫著「真實消費金額 `57,873` →
`5x,xxx`」——**記錄塗銷的那句話本身把數字又寫了一次**。已改掉。
寫「我塗掉了 X」的時候不要把 X 寫出來。
