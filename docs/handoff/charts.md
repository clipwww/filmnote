# 交接筆記 — 圖表與對外頁面（charts，第二棒）

> ⚠️ **這個檔名被 `main-session.md` 與 `adminui.md` 引用著，不要另開新檔。**
> 上一棒的 session 名字叫 `adminui`（歷史遺留），它的 `/admin/**` 範圍在
> `docs/handoff/adminui.md`，仍然有效但不是這一棒的範圍。

## 0. 範圍與這一輪的 commit

**擁有**：圖表元件（`BaseChart` / `ChartBand` / `ChartLegendSwatch` /
`AttendanceCalendar` / `HourHeatmap` / `MonthlyTrend` / `DistributionBars` /
`SpendByYear` / `YearStrip`）、`app/utils/{chart-theme,stats}.ts`、
`app/composables/{useYearStats,useUserSpend}.ts`、
`app/pages/{index.vue,app/index.vue,u/[username].vue}`、
`server/api/u/[username]/stats.get.ts`。

⚠️ **2026-09-07 追記**：上面那張清單漏了 `RepeatList`（band 7，見 §3.1），
而這一輪新增了 `app/utils/hour-heatmap-option.ts`（`HourHeatmap` 的 option 建構，
見 §1.7）。兩個都在圖表線的範圍內，`git add` 的時候不要漏。

**共用檔，動之前要問**：`nuxt.config.ts`、`package.json`。
⚠️ **`docs/design/SCREENS.md` 現在是主 session 的**——要改規格就寫替換稿到
scratchpad 交給他貼，不要自己動也不要 `git add`。

```
7759272  /app 檢視視角改成全期預設，平均線接回 monthly_baseline
044b4a7  /u/ 補上整組圖表，修掉搬過去才炸出來的 hydration mismatch
051181d  /u/ 加上「每年花費」，三種觀看者各驗過一次
4c27b68  「最」要是真的——圖說門檻改成守「尖峰站不站得出來」
```

**收工時的狀態**：`pnpm test` **334**、`pnpm typecheck` 綠、`pnpm run lint`
exit 0、`pnpm verify:all` **51／51**（我接手時是 36／36、test 318）。

---

## 1. 這一輪做完的，以及**為什麼是這樣**

### 1.1 全期視角是預設（David 2026-09-06 裁決）

**這比翻一個預設值大。** 改之前 UI 上**根本到不了**全期，而程式看起來支援：
`YearStrip` 的 emit 型別是 `[year: number]`（永遠 emit 不出 null）、
`activeYear` 起始就是 `available_years[0]`，所以模板裡那些
`activeYear === null ? …` 全部是**跑不到的死分支**。
`AttendanceCalendar` 真的進到全期會去畫一張「0 年」的日曆。

四條落實方式，每一條都有理由：

1. **「全部年度」是 `YearStrip` 最上面**看得見的一列**（有 `aria-selected`、
   鍵盤走得到、預設選中）。刻意**不做成「再點一次同一年回到全部」**
   ——年表是這一頁的檢視選擇器，而**選擇器不能有一個選不到、只能猜出來的狀態**，
   尤其它還是預設值。
   ⚠️ 那一列**刻意沒有 53 格**：53 格的座標軸是「一個日曆年裡的第幾週」，
   跨年度沒有這個座標；把十三年疊起來畫會是一條幾乎全滿的黑帶，
   既不傳達東西又會被讀成「這是某一年」。
2. **band 2 年度出席圖在全期整條不出現**。「隱藏」不是「畫成空的」也不是
   「畫成 0 年」。
3. **每一句圖說都要說得出自己涵蓋什麼範圍**（`scopeLabel`）。同一句話在兩種
   視角下長得一樣而數字差十倍，是最容易被讀成「資料錯了」的形狀。
   band 7 多刷排行尤其要注意：語意會從「那一年內看過兩次」變成「這些年來看過兩次」。
4. **band 4 全期時不畫平均線**。全期的實線是「十三年同月加總」（十月 24），
   虛線是同一組數字除以曝光數（2.00）——同一條 y 軸上虛線會貼著底趴平，
   而且它在全期**不再具有對照功能**：它就是實線本身除以一個常數。

**頁面的標題數字**（「總共看了 174 場、250 張票」）原本是 band 2 的 caption，
已提到**頁面層級**——否則全期視角下它會跟著 band 2 一起消失，
而那是全頁唯一說得出總數的地方。

### 1.2 ⚠️ band 3 時段熱點圖的「永遠看全部年度」特例**已經拿掉**

`SCREENS §9c.2` 原本要求把它釘死在全部年度。**那個特例 2026-09-06 刻意拿掉了**，
因為它的前提消失了：原本的問題是「時段圖只吃到 8 筆」，根因是**當時的預設是
最新年度**。全期變成預設之後，原本要靠特例達成的效果由預設值本身達成了。

而特例留著會產生更糟的症狀：使用者切到 2019 年時，全站就只剩這一張還在看十三年，
畫面上沒有任何標記說明它跟別人不同。**「唯一一張跟年份選擇器唱反調的圖」比
「樣本小的圖」更難自己看出來。**

⚠️ **不要把它加回來。** 要加回來的話，圖上必須同時出現一個看得見的標記說明它的
範圍跟頁面其他部分不同，而且**抽屜的過濾要跟著改**（不改會出現「點一格說 8 場、
抽屜列出 24 張卡」）。`SCREENS §9c.2` 已更新，原文保留在下面當紀錄。

### 1.3 ★ 平均線的分母本來是錯的（§7 #165）

`0003` 為這件事特地做了 `monthly_baseline`（分母是**曝光數**：該月份實際經歷過
幾次），`verify-core` 的 H4／H6 守得很死。但**那個欄位在整個前端 `grep` 是空的**
——`app/utils/stats.ts` 自己用「該月份總場次 ÷ 年份數」另算了一套，
12 個月裡有 5 個偏（十月畫 1.85，DB 說 2.00），而 H1–H6 全綠、單元測試也全綠。
**舊的單元測試甚至把錯的答案釘住了**（`expect(avg[9]).toBe(1.85)`），
所以它是反向的保護。

⇒ `monthlyBaselineSeries()` 現在**只做搬運，不做計算**。
⚠️ **不要因為「自己算比較直接」而把計算搬回去。**

守它的是 `verify-all.ts` 的三條**跨層**斷言（DB 那側守不到）：
① 真正的前端函式跑真實資料 vs DB 的 `monthly_baseline` 逐格對帳；
② **對照組**：證明那組資料分辨得出兩種分母（沒有這一條，資料一變成兩種分母
同解就會靜默失去分辨力）；③ 凡是 render `<MonthlyTrend` 的頁面都必須真的呼叫
`monthlyBaselineSeries`。三條都故意弄壞過確認會紅。
⚠️ ③的第一版是假綠燈——**註解裡的函式名餵飽了字串比對**（§7 #166），
strip 掉註解之後才真的會紅。

### 1.4 `/u/` 的整組圖表

分享是這個產品唯一的擴散機制，而 `/u/` 一張圖都沒有、`/app` 有七條 band。
現在兩頁同一組 band、同一個檢視視角語意。

- **資料一律走 `/api/u/{username}/stats`（明確使用 anon client）**，
  不在前端拿紀錄列表就地算。年表本來是拿 `items`（上限 200 筆）算的，
  超過的人**年表會缺格子而且沒有任何提示**。那支端點與列表走同一條匿名路徑
  ⇒ **圖與列表永遠是同一個母體**。
- **那支端點逐欄挑白名單，金額欄位一個都不回。** 匿名視角下它們一律是 0，
  而 **0 比不給更糟——會被讀成「這個人沒花錢」**。
  ⚠️ **白名單不是黑名單**：日後 RPC 多一個帶金額的欄位（它一定會，那支同時
  服務 `/app`），黑名單會自動放它出去。**失敗方向不同**：黑名單漏一個＝洩漏，
  白名單漏一個＝少一張圖。**少一張圖看得見，洩漏看不見。**
- **不快取，也不要加 `defineCachedEventHandler`**（踩雷 #1 的同一族）。

### 1.5 「每年花費」（David 2026-09-06 裁決，推翻了我原本「不做」的預設）

⚠️ 這一節原本的標題是「**`/u/` 的**每年花費」。**2026-09-07 起 `/app` 也有這條
band**，所以範圍限定拿掉了——底下 2026-09-06 的每一條判斷都沒有被推翻，
只是現在同時適用於兩頁；兩頁不同的地方寫在本節最後三條。

**「時有時無」是要設計的東西，不是要迴避的東西。** 三種觀看者都必須是對的：
本人看得到全部／`show_cost = true` 的路人看得到公開紀錄的票價／
其他人**一列都拿不到 ⇒ 整條 band 與頁首那句都不存在**。

- **判斷不由前端做，由 RLS 做。** 判準是「**讀得到幾列票價**」
  （`spend_known_records > 0`），不是「總額大於零」——後者會把
  「全部都是兌換票（NT$0）」誤判成「看不到」。
- **不存在就是不存在**：不畫成 0、不打馬賽克、**不留佔位**。band 排在堆疊最後，
  而且**刻意不給 `#fallback` 骨架**——給了的話拿不到金額的觀看者會看到一塊
  先撐開再塌掉的空白，那不但是版面跳動，還等於公告「這裡本來有東西」。
- **`spend_is_partial` 兩層標記，缺一不可**：① 數字寫「NT$6,890 **以上**」
  （**跟著數字走**，使用者只截到一列時但書仍然在）；② 長條右緣**虛線開口**
  （形狀也在說「還沒完」，不能只靠色差——墨階相鄰階低於 WCAG 1.4.11 的 3:1）。
  用**逐年**旗標：全期那個只要任何一年有未公開票價就是 true，等於沒標。
- ⚠️ **NT$0 不等於隱藏**（§7 #171）。第一版寫 `filter(spend > 0 || partial)`，
  把 David **2015 年**（2 場、票價都記了、合計 NT$0 兌換票）整列吃掉
  ——**年表上有那一年、花費圖上沒有**，而畫面看起來完全正常。
  現在：有紀錄就列，`0 且完整` 顯示「免費」且**條寬為 0**（吃到最小條寬的話
  右邊寫免費、左邊畫一小段條，圖與字互相矛盾）。
  ⚠️ 但 `0 且不完整` **仍然**是「NT$0 以上」——把它也寫成免費就是
  **把「沒公開」講成「沒花錢」**。
- **金額只存在於 client**（`useUserSpend()`，`server: false`）。頁首那句與這條
  band **共用同一次請求**（同一個 `useAsyncData` key）：兩份查詢一定會在某次
  修改後對「看不看得到」「是不是全部」給出不同答案，而**沒有人會把同一頁的
  兩個地方擺在一起看**。
- **★ 2026-09-07 起 `/app` 也有這條 band，而兩頁的資料來源刻意不同。**
  `/u/` 走 `useUserSpend()`（觀看者自己的 session、`server: false`、外面包
  `<ClientOnly>`——那一頁是 SSR，金額不能進 `__NUXT_DATA__`）；
  `/app` 走那一頁**本來就有的** `allStats.by_year`，不包 `<ClientOnly>`
  （`/app/**` 在 `nuxt.config` 是 `ssr: false`，整頁本來就是 client-only）。
  ⚠️ **`/app` 刻意不引入 `useUserSpend()`。** 那支存在的兩個前提在 `/app` 都不成立
  （那一頁不是 SSR、觀看者永遠是本人），搬過來只會多發一次一模一樣的 RPC，
  並且製造**同一頁上兩個金額來源**——頁首那句「花了 NT$…」與這條 band
  一定會在某次修改後對不起來，而**沒有人會把同一頁的兩個地方擺在一起看**。
  （那正是 `useUserSpend()` 檔頭在防的東西，不要把它搬過來又親手破壞它。）
  ⚠️ 而且**一定要用 `allStats` 不是 `stats`**：RPC 在 `p_year` 不是 null 時
  `by_year` 回的是**空陣列**，寫成 `stats.value?.by_year` 的話使用者一切到某一年
  這條 band 就無聲消失，而畫面看起來完全正常。
- **★ 每一列是三段：`{金額} / {場數} 場 / {票數} 張`**（David 2026-09-07 逐字指定
  的顯示格式）。金額由 `spendText()`、後兩段由 `spendCountsText()`。
  ⚠️ **分隔是斜線，不是站內慣例的全形空白**——這是刻意違反 `DESIGN_SYSTEM §0`
  與本檔 §2.2 的量詞串慣例，因為 David 逐字寫的是斜線，先例是 `UserSpendSummary`
  的「只涵蓋 3 / 5 筆紀錄」。**不要順手改成全形空白或中點**（中點串是 Letterboxd
  的簽名，這個產品刻意不長那樣）。
  ⚠️ **場數與張數永遠是完整的，不可以跟著 `isPartial` 變灰。**
  `spend_is_partial` 說的是「有幾筆**票價**讀不到」，場次與張數一筆都沒少。
  把兩個完整的數字染上「不完整」的視覺訊號，是「把沒公開講成沒花錢」的鏡像。
  ⇒ `text-highlighted / text-muted` 的條件式**只掛在金額那一段**。
  ⚠️ 分隔的斜線必須是**兩個 span 之間的純文字節點**，`whitespace-nowrap` 只掛在
  前後兩個原子片段上：**nowrap 內部的空白不產生斷行點**，把「 / 」寫進 nowrap
  span 裡的話整個右側會變成一段不可斷的文字，外層是 flex、沒有 overflow-hidden
  ⇒ 375px 放不下時直接橫向溢出。反過來也不行——斷點**絕不可**落在數字與量詞之間
  （「27」與「張」分家），那是 `utils/ticket.ts` 檔頭記過的「影城 數位」那個病。
- **⚠️ 「兩頁有沒有把 `tickets` 傳進去」不要用 grep 守。**
  `app/pages/u/[username].vue` 全檔**零筆** `tickets`（那個數字是 `useUserSpend()`
  的 map 帶進去的），而 `app/pages/app/index.vue` **本來就有三處無關的** `tickets`
  ⇒ 正反兩個方向都會壞：守 `/u/` 會永遠紅，守 `/app` 會永遠綠。
  那件事交給 typecheck（`SpendByYear` 的 `tickets: number` 是必填 prop）。
  這是 §5 那條「這個專案的錯幾乎都是檢查機制本身失效」的又一例。

### 1.6 ★★ 「最」要是真的（§7 #174／#175）

這一節是這一輪最值得留的東西。

舊門檻 `INSIGHT_MIN = 20` 守的是**總筆數**，而它守不到真正的問題。
實測 David：174 筆、67 個有值的格子，圖說仍然是
「你最常在週六 10:00 進場，共 9 場」——而他的前三高是
**週六 14:00 = 9、週六 10:00 = 9、週五 22:00 = 8**。

> **前兩名並列。那句話是在並列裡任意挑一個。它不是弱，是不成立。**

⚠️ **只看「尖峰夠不夠大」會放行**：9 場在 mean 2.6／sd 2.16 之下確實是離群值，
z 分數那一類的檢定會過關。**「最」宣稱的是唯一性，所以判準也必須是唯一性，
不是大小。** 這是這一輪我學到最重要的一句。

⇒ `peakStandsOut()`：**至少 5 場**（低於這個不是習慣是巧合）**且 ≥ 1.5 倍
第二名**。兩條缺一不可，各自弄壞驗過會紅（拿掉領先幅度 → 3 條紅；
拿掉下限 → 1 條紅）。

**同一個病在聚合那一層又發生了一次**：第一版聚合只要求「高過平均分佈 1.4 倍」，
於是畫面上出現「**你 39% 的場次在週五到週日的晚上**」——基準線 30%，
數學上沒說錯，但 **39% 是少數**，使用者讀到的是「這就是我的樣子」而 61% 不是。
⇒ 加 `AGGREGATE_MIN_SHARE = 50`：**聚合句必須真的在講多數**。
同一組資料改講真的成立的那句：「**你 79% 的場次在週五到週日。**」

**判準是「這句話講出去要是對的」，不是「數學上站得住」。**

三層，由穩健到脆弱：穩健聚合 → 站得出來的尖峰 → 純敘述
（「沒有特別集中的一格」本身就是一個發現，而且是真的）。
兩頁共用同一支（`hourInsightText` / `venueInsightText` 在 `utils/stats.ts`）。

**David 真實資料上的實際輸出**：

| 檢視 | 時段 | 去了哪裡 |
|---|---|---|
| `/app`＋`/u/` 全部年度 | 你 79% 的場次在週五到週日。 | 你的主場是 林口MITSUI OUTLET PARK威秀影城，69% 的場次在這裡。 |
| `/app` 2019（25 場） | 2019 年的 25 場散在 21 個時段，沒有特別集中的一格。 | 你的主場是 …，84% 的場次在這裡。 |
| `/app` 2015（2 場） | 2015 年的 2 場分佈在 2 個時段。 | 2015 年的 2 場分佈在 1 個場所。 |

⚠️ **如果 David 覺得門檻嚴到什麼都不講了，那是要調的參數，不是要放棄的原則。**
三個常數都可調，但**放寬之前先確認放寬之後那句話仍然是對的**。

### 1.7 ★ 時段熱點圖的兩條總和軌道（David 2026-09-07 需求）

**做了什麼**：格盤外側多兩條總和——底部一列是每個星期的總和，右側一欄是每個
時段的總和。兩條都可以點，開的是 §9.2 那同一個 `UDrawer`。

**★★ 為什麼是軸標籤而不是格子**（這一項唯一真正致命的分歧點）：
實測 David **單格最大 9、欄總和最大 60**。

- 總和進 `data` ⇒ `max` 變 60 ⇒ `heatPieces(60)` 的第一段 `{ gt: 0, lte: 10 }`
  把 112 格**全部**塌進最淺的兩階，整張圖失去分辨力。
- 總和沿用 `max = 9` ⇒ `{ gt: 7 }` 把總和塗成跟尖峰一模一樣的顏色，
  總和看起來就像另外七格資料。

**兩種都零錯誤訊息、畫面都看起來很正常。** ⇒ 總和只能是格盤外側的軸標籤。
`hourGrid()` 的 `colTotals` / `rowTotals` **跟 `data` / `max` 完全分開累加**，
就是為了守這件事——不要為了「少一個迴圈」把它們併回去算。

**★★ `triggerEvent` 有一個沒人寫過的副作用。** ECharts 的 `AxisBuilder` 在
`if (triggerEvent)` 區塊**之外**無條件呼叫 `setTooltipConfig()`，對**每一個**軸標籤
都塞了 `ecData.tooltipConfig`；標籤一旦因為 `triggerEvent` 變成 non-silent，
`TooltipView._tryShow` 就把它當成 component dispatcher ⇒
**滑過任何一個軸標籤都會冒出一個只寫著「六」或「60」的泡泡。**

⇒ **每一條加了 `triggerEvent` 的軸都必須同時寫 `tooltip: { show: false }`。**
那不是美化，是必要配套；而且它**不會**把標籤變回 silent
（`isLabelSilent()` 看的是 `triggerEvent || tooltip.show`），點擊仍然成立。
⚠️ 順帶更正一則流傳的錯誤情報：**全域的 `tooltip.formatter` 不會被軸標籤呼叫**
（ECharts 明文寫著 component tooltip 不共用 series 的 formatter），
所以「軸標籤會印出『週 undefined』」那個擔心永遠不會發生，
不要為它加防禦性的 guard。

**★★ 新的驗證手段——比這一項本身更值得帶走。**
`echarts.init(null, null, { renderer: 'svg', ssr: true, width, height })`
＋ `renderToSVGString()` 在**純 node 裡**就跑得起來：不開瀏覽器、不佔 port、
不需要 node-canvas，可以直接放進 `pnpm test`（現在在 `tests/stats.test.ts`）。

它抓得到三件「對 option 欄位下斷言」抓不到的事：
① 標籤被 `axisLabel.interval: 'auto'` **靜靜丟掉**（掉一個總和的症狀是
「那一格看起來是 0 場」，零錯誤訊息 ⇒ 兩條總和軸都寫死 `interval: 0`）；
② `inverse` 反了（右側 16 個總和整組上下顛倒，而每一個都是合理的數字）；
③ formatter 的索引 off-by-one。**後兩者都是「每個數字看起來都合理」的靜默錯誤。**
斷言的形狀是：右側總和與左側時段標籤**逐列同一個 y**，直接比兩排的 y 陣列。

⚠️ 對 option 欄位下斷言（`grid.right === 36`、`yAxis[1].inverse === true`）大半是
**照抄原始碼型的變更偵測器**：改了數字就紅，但它答不出「36 夠不夠」
「總和有沒有跟列對齊」。兩種都要，但要知道哪一種在守什麼。

**為什麼 option 搬進 `app/utils/hour-heatmap-option.ts`**：SFC 進不了 vitest，
而上面那個 render 斷言需要一個純函式。
⚠️ **用新檔，不要把既有的匯出搬過去**——踩雷 #173 的形狀正是「搬動匯出讓
dev server 的模組圖沒更新，四個檢查全綠而 `/u/` 一條 band 都畫不出來」。
`HourHeatmap.vue` 也因此**顯式 import** 那兩個匯出，不靠 auto-import；
那個檔本身也不靠 auto-import 取 `chartPalette`（vitest 的 `~` 指向 `src/`，
裸用會是執行期 ReferenceError，而 `pnpm dev` 與 typecheck 都正常）。

⚠️ **高度公式從 `+40` 改成 `+60`**（上 24 給星期軸、下 26 給新的星期總和軸）。
**忘記改的症狀不是「底部那列被切掉」**——SSR render 實測：高度少 20 時底部總和
照樣畫得出來，一個字都沒少。真正的症狀是每一列悄悄從 22.625px 矮成 21.375px。
⇒ **任何寫成「檢查底部總和沒有被切掉」的驗收，弄壞了也會綠。**
`/u/` 的 `<ClientOnly> #fallback` 骨架綁的是同一支 `hourHeatmapHeight()`，
不要再手抄一次數字。

### 1.8 「去了哪裡」的長尾可以展開（David 2026-09-07 需求）

`DistributionBars` 的「其他 N 家」那一列現在可以點開，就地列出被收進去的長尾。
四個決定，每一個都有理由：

1. **★★ 展開出來的長條用的是同一把尺（第一列 ＝ 100%），不可以換成長尾自己的
   最大值。** 實測 David 全期：「其他 10 家」裡 3 場的那一條若拿自己當滿版，
   會被畫得跟主場一樣長——**那比不展開更糟，因為它看起來像資料**。
   代價是展開後看到十根幾乎一樣短的小刺，那是誠實的代價（David 2026-09-07 裁決）。
   ⇒ 收合列與展開列共用**同一個** `barWidth()`，巢狀那一層不重算基準。
2. **`limit + 1` 併進 head，不聚成「其他 1 家」**（`topWithRest()`）：
   點開只有一列的展開鈕比直接多畫一列還糟。而且這不是假想的邊界——實測 David
   全期的國別與版本**正好都是 5**（＝呼叫端傳的 limit），
   距離踩到它只有一筆紀錄。
3. **換年份一定要收回去。** 實測 13 個年度裡只有 2016（7 家）與全期（15 家）
   長得出「其他」那一列，其餘 11 年 ≤5 家、那一列根本不存在。
   ⚠️ **watch 的是 `props.items` 這個陣列參考本身，不要「化簡」成 `items.length`**
   ——全期是 6 列（5 ＋ 其他 10 家）、2016 也是 6 列（5 ＋ 其他 2 家），
   **長度一模一樣**。用長度當 source 的話「全期（展開中）→ 2016」這一步不會重置，
   而那正是這個 watch 唯一要擋的 bug。
4. **是按鈕，不是原生的揭露元素。** ① 展開狀態必須由 Vue 擁有——兩個頁面都沒給
   這支 `:key`，換年份不 remount，而原生揭露元素的開合是 DOM 狀態，
   要收就得綁 `:open` 再接 `toggle` 同步回來，比一個 ref 多兩個會對不起來的地方；
   ② 原生揭露元素的標題列會把**整塊**變成觸發區，而這裡一列是「名稱＋數字」一行、
   「長條」一行。用按鈕才能把觸發區精準地只包住第一行。
   > **2026-09-14 更新：這一條的舊理由已經被推翻。** 舊文寫的是「整列可點就等於宣告
   > 『這條長條可以點』，**而其他長條一條都不能點**」——那個前提沒有了：**現在每一列
   > 都可點**（David 第 4／5 點）。新的理由是 `SCREENS §9.5 規矩 4／規矩 5`：兩種手勢
   > （聚合列＝展開、一般列＝開抽屜）都掛在**名稱那一行**、靠箭頭區分，長條那一行
   > 一條都不吃點擊——長條是**資料**不是控制項，而聚合列的長條畫的是加總。
   > 結論（用按鈕、觸發區只包第一行）沒變，只有理由換了。
   展開清單的 id 用 `useId()`：`/u/` 是 SSR，自己編 id 就是 hydration mismatch，
   而**屬性的 mismatch Vue 不會幫你修**（踩雷 #98）；也不可以提到模組範圍，
   同一頁有三個實例（影城／版本／國別），共用一個 id 會讓關聯屬性指到別人的清單。

⚠️ **`DistributionBars.vue` 的檔頭刻意避開那幾個標籤名與屬性名的字面 token**
（踩雷 #166：`src.includes(…)` 型的字串比對會被註解裡的同名字串餵飽，
於是呼叫被整個拿掉照樣綠）。**要守這個檔請解析 DOM，不要比字串。**

---

## 2. ~~★★ 任務 2：首頁第二段「剛剛有人看了」~~——**2026-09-07 David 裁定不做**

> ### 🔴 這一整節是**已結案的待辦**，不要照著施工
>
> David 2026-09-07：「**首頁先這樣就好，不用再加東西了**。」
> 裁決全文與理由寫在 `SCREENS §1` 那則 🔴 框裡。
>
> **這一節保留而不刪，是刻意的**——它的論點（最有說服力的東西是台灣人剛剛看了什麼）
> 沒有被推翻，被結案的是**待辦**，不是**論點**。日後真要重啟 ②，下面整節的硬約束、
> 六個坑、與斷言設計仍然全部有效，而且是唯一寫下來的地方。
>
> ⚠️ **但下面有三處指示已經被那則裁決反轉，照做就是做錯事**（2026-09-08 文件稽核抓到）：
>
> | 這一節寫的 | 現在的事實 |
> |---|---|
> | §2.2「頁尾規模數字…**順手補齊**」成三個 | **維持兩個，不補第三個。** 補第三個也是「加東西」。版面圖保留原樣當作日後要補時的規格 |
> | §2.5-2「首頁要不要顯示 `memo`…**請先問 David**」 | **他已經答了：不要。** `memo` 是全站內容審核風險最高的欄位 |
> | 節首「這是唯一沒動的大件」 | 2026-09-07 那一輪動了十一項，這一節反而是唯一**刻意不動**的 |
>
> ⚠️ 給下一棒：第二棒與第三棒的交接筆記都把這一項列為「下一棒的第一件事」。
> **那兩份寫在這則裁決之前，不要照做。**


這是唯一沒動的大件，也是這份筆記最重要的一段。

### 2.1 現況（別重做一次已經做好的東西）

⚠️ **前一棒的交接寫「首頁尚未實作、需要 backend 先給端點」，那兩句都是錯的。**
`SCREENS §1` 的兩層結構中，**① 海報牆 hero 已經完成並上線**：
`/api/posters` 正常、160 張熱連結海報、1280 與 375 都無橫向溢出、純白 0。
**沒做的只有 ② 底下接近期公開紀錄。**

### 2.2 `SCREENS §1` 要求的形狀

- 區塊標題「剛剛有人看了」，底下是**票根卡**列表。
- ⚠️ **首屏第一張刻意是一張「沒有海報」的票根卡**（版面圖裡就是這樣畫的）。
  那是刻意的：**它讓「無海報是常態」在第一眼就成立**（`DS §0` 的驗收標準是
  「無海報的卡片要好到使用者不會希望它變成海報」）。
  ⇒ **挑資料時不要讓它變成一面海報牆**，那會把這一段做成 Letterboxd。
- 頁尾規模數字目前是兩個（`N 部台灣上映作品　N 家可選影城`），
  `SCREENS §1` 的版面圖寫的是**三個**：**部作品／家電影院／個影廳**。順手補齊。
  分隔用**量詞 + 全形空白**，不是中點（中點串是 Letterboxd 的簽名）。
  ⚠️ 全形空白要寫成 `　` 跳脫，字面貼進原始碼會被 lint 擋。

### 2.3 硬約束（`/` 是 `isr: 300`）

1. **必須經一支明確使用 anon client 的 Nitro 路由取得**（`publicSupabase()`），
   不要用 `serverSupabaseClient(event)`——後者會帶 cookie，RLS 依觀看者而異，
   而 ISR 以**路徑**為快取單位 ⇒ 第一個訪客的版本被烤進 CDN（踩雷 #1 的家族）。
2. **票價絕不進 payload。** 照 `/api/u/[username]/stats` 的形狀做：
   **逐欄挑白名單**，不要把查詢結果整包轉發。
3. **任何隨來訪者而異的東西只能在 client 端決定。** 現有的 CTA 已經用
   `<ClientOnly>` + **未登入的 fallback** 處理好了，照同一個形狀做——
   `#fallback` 給的必須是「未登入」那一版，這樣即使中介層那道防線失效，
   被烤進 CDN 的也只會是未登入。
4. 海報一律熱連結 `image.tmdb.org`，不建 proxy、不轉存。**這是合規不是效能。**

### 2.4 要補的斷言（以及**它必須配一組反向的**）

打算補的是「首頁 payload 裡沒有身分也沒有金額」。
⚠️ **只驗這一條會被「中介層把所有東西都拔光」矇混過去。**
`scripts/verify-http.ts` 那一組已經有現成的形狀可以照抄——它有一條反向斷言：
**不快取的 `/u/**` 上「必須看得到」token**，證明偵測方式是對的。

⇒ 同理要有：**「首頁的近期紀錄區塊確實有內容」**（有 N 張卡、有片名），
否則「payload 乾淨」可能只是因為那個區塊根本沒 render。

### 2.5 ⚠️ 我已經想到但還沒寫進程式的坑

1. **「剛剛有人看了」的資料來源必須是全站公開紀錄，不是某個人的。**
   `viewing_record_public` view 已經處理了 visibility 與取下，但**它沒有處理
   「作者被終止服務」以外的排序問題**：要照 `watched_on desc, id desc` 排
   （只以日期排序時跨頁順序不保證——backend 為 `/api/u/` 加 `id` 破平手就是這個原因）。
2. **⚠️ 這一段會把陌生人的觀影紀錄放在首頁。** 那是 `SCREENS §1` 明文要的
   （「最有說服力的是台灣人剛剛看了什麼」），但它同時是**唯一一個把使用者內容
   推到未登入首頁的地方**。`memo` 是使用者自由文字且預設公開——
   **首頁要不要顯示 `memo` 是產品決定，不是技術決定，請先問 David。**
   我的建議是不顯示：首頁的卡片只需要片名／影城／時間就成立，
   而 `memo` 是這個站上內容審核風險最高的欄位（`§90-4` 的通知取下流程管的是它）。
3. **ISR 300 秒 ＋「剛剛」這個詞。** 快取五分鐘代表最新那筆可能是五分鐘前的，
   文案上沒問題；但**如果全站很久沒有新紀錄，這一段會長期顯示同一批**，
   讀起來會像站是死的。⇒ 至少要有一個「資料不足時整段不出現」的分支
   （同 `§9.3` 的精神：不畫空的東西）。
4. **卡片的年份**：首頁的列表沒有依年份分組，所以 `TicketCard` 要傳
   **`show-year`**——同抽屜的理由（`§7` 那條記過：日期帶預設不顯示年份，
   是因為列表由上下文提供年份，而這裡沒有那個上下文）。
5. **量對比不能只看 CSS 往上找背景色。** 這一段在海報牆下方，如果它被放進
   hero 的視覺影響範圍內，就要照 §3 的方法把整疊合成起來算
   （實測差距：假 4.99:1 vs 真實 4.11:1）。
6. **`/api/posters` 已經是 `defineCachedEventHandler`**；新端點**不要**照抄那個
   ——近期紀錄要跟得上 ISR 的 300 秒，再疊一層自己的快取只會讓兩個 TTL 打架。

---

## 3. 任務 3：line / bar 的點擊展開——**2026-09-07 做了一半**

⚠️ 這一節原本的第一句是「**現在只有出席圖與時段熱點圖可以點**」。
**那句話已經過時了**（多刷排行與熱點圖的兩條總和軸現在都可以點），
**但底下那幾條注意事項一條都沒有被推翻**——它們現在是「**已經被滿足過一次的**
注意事項」，留在這裡給還沒做的那一半用。不要因為「多刷做完了」就把整節刪掉。

### 3.1 已經做完的一半

- **多刷排行（band 7）整列可點**，接的是 §9.2 那**同一個** `UDrawer`。
  底下四條注意事項在它身上**全部適用，而且已經照做**：
  1. **抽屜裡的 `TicketCard` 傳 `show-year`**——實測 David 排第一的那部片，
     10 次散在 **2016／2017／2018／2024／2026 五個年份**。
  2. **`/u/` 走 `pick()`，先 `await ensureAllRecords()` 再開抽屜**（踩雷 #169）。
  3. **抽屜的過濾用 `inRepeatScope(r, filmId, activeYear／selectedYear)`**，
     跟 band 7 吃的 `stats` 是**同一個 scope**。少了年份會變成
     「排行說 3 次、抽屜列 4 張」，**而排行那個數字自己是對的**。
     （`/u/` 那邊還多一條：過濾的是 `cards` **不是 `visible`**——後者是下方列表的
     分頁切片，用它的話抽屜會被「使用者按過幾次『再顯示 24 筆』」悄悄截斷。）
  4. **每一列都有東西可展開**——`repeats` 的 `having count(*) > 1` 保證 ≥2。
  **實作細節**：整列是一顆 `<button>`，**片名的 `NuxtLink` 已經拿掉**
  （`<a>` 巢在 `<button>` 裡是無效 HTML，而且點到片名時連結會吃掉事件，
  使用者只會覺得「有時候點得開、有時候點不開」）。作品頁的入口改由抽屜裡
  `TicketCard` 的片名提供——**那個 slug 來自紀錄、過了 public + approved 的閘門，
  而排行的 `repeats[].slug` 直接來自 `left join film`，沒有過閘門。**
  另外 `FilmPoster` 那一層加了 `aria-hidden`：包進按鈕之後它的 alt／aria-label
  會被吸進按鈕的可及名稱，螢幕閱讀器會唸成「XX 海報，XX，10 次」。
- **`DistributionBars` 的「其他 N 家」可以展開**（見 §1.8）。
  ⚠️ 那一個手勢**不是** `@pick` 抽屜，是就地展開長尾。
  > **2026-09-14 更新：這支現在有 `@pick` 了**（`DistributionBars.vue` 的
  > `defineEmits<{ pick: [item: { key, name, records }] }>`）。舊文這裡寫的是
  > 「**這支至今仍然沒有 `@pick`**」——已作廢。兩種手勢怎麼分見 §3.3 與
  > `SCREENS §9.5 規矩 4`：**聚合列**（`key === null`）＝展開／收合，
  > **一般列**＝開抽屜，兩者都在名稱那一行、靠箭頭區分。
- **時段熱點圖的兩條總和軸可以點**（見 §1.7），開的也是同一個抽屜；
  星期總和的過濾述詞 `matchesWeekdayPick()` 刻意放在 `utils/stats.ts`，
  因為它含一條真實資料測不出來的條件（熱點圖的母體不含沒記時間的紀錄，
  而 David 的 `records_without_time` 是 0）——寫成頁面裡的 inline computed
  就沒有任何測試守得住它。

### 3.2 ~~還沒做的一半：`MonthlyTrend` 的 `@pick`~~ — **2026-09-14 結案，已實作**

> ⚠️⚠️ **這一節是已完成的事，不是待辦。** 舊文寫的是「`MonthlyTrend` **至今沒有任何
> emit**」——那句話在 2026-09-14（David 第 5 點）就不成立了：
> `MonthlyTrend.vue` 現在 `emits: { pick: [month: number] }`（1..12），x 軸另外開了
> `triggerEvent: true` 讓 12 個月份標籤也點得到（`tooltip: { show: false }` 是那個
> 開關的必要配套，不是美化），兩頁的接線是 `/u/` 的 `pickMonth()` 與 `/app` 的同名函式。
> **不要照這一節再做一遍。** 底下五條是當時列的施工要求，**每一條都已經落地**，
> 留在這裡是為了讓下一棒能逐條反查（弄壞任何一條都不會有東西變紅）：

- ⚠️ **每一個資料點都要有東西可展開。** 平均線上的點不對應任何紀錄，
  那條 series 已經設 `silent: true`——**點了沒反應的圖表比不能點更糟**。
- ⚠️ **抽屜裡的 `TicketCard` 一定要傳 `show-year`。** 月度趨勢的抽屜跨的年份
  **比出席圖與時段圖更多**（多刷已經替這一條做了證明：一部片就跨了五年）。
- ⚠️ **`/u/` 上要先 `await ensureAllRecords()` 再開抽屜**（見 §4-3）。
- ⚠️ **抽屜的過濾必須跟圖的 scope 一致**。指定年份時月度抽屜只能列那一年。
- ⚠️ 抽屜的 `Picked` 是一個 discriminated union，每多一個 `kind` 就要同時補
  **標題**（`drawerTitle`）與**空狀態文案**（`drawerEmptyText`）——
  「這個時段沒有紀錄」對一個月份是錯的，而空抽屜會理直氣壯地說謊（踩雷 #169）。

### 3.3 ★ 「其他」那一列的兩個點擊語意——**2026-09-14 已裁決**

> ⚠️⚠️ **舊文在這裡留了一個「必須先決定怎麼分」的待決，那個決定已經做了，
> 而且做成跟舊文舉的例子相反的。照舊文施工＝直接推翻 `SCREENS §9.5 規矩 4`。**
>
> **最終切法**（`SCREENS §9.5` 規矩 4／規矩 5，`DistributionBars.vue` 檔頭是同一份）：
> - 兩種手勢**都掛在「名稱＋數字」那一行**的那個 `<button>` 上，靠**箭頭**區分：
>   聚合列（`key === null`）有展開箭頭＝就地展開長尾（`aria-expanded` + `aria-controls`）；
>   一般列沒有箭頭＝開底部抽屜（`aria-haspopup="dialog"`）。聚合列**不 emit `pick`**。
> - **長條那一行一條都不吃點擊**，聚合列的也不吃。
> - 展開出來的長尾**不縮排**（規矩 5，David 第 3 點）；分隔靠 `border-t` + `pt-3`。
>
> **被否決的：舊文舉的「抽屜只掛在長條上、展開只掛在名稱那一行」。**
> 理由：長條是**資料**不是控制項（點資料本身沒有語意，而且 375px 下短長條只有 2%
> 寬、根本點不到），聚合列的長條畫的又是**加總**，點它要開的抽屜根本不存在。

現在的 markup 就是按這個切的（按鈕只包住「名稱＋數字」那一行，長條在按鈕外面），
**不要為了省事把整列包成一個觸發區**。

⚠️ 另外，`DistributionBars` **不是圖表庫畫的，是 HTML 排版** ⇒ 加 `<button>` 語意，
**不要**為了統一而把它改回 canvas（長中文影城名在 375px 下會被截斷，
而且鍵盤與螢幕閱讀器會拿不到）。

---

## 4. 未驗證 / 已知限制（誠實清單）

1. **觸控裝置上點一格會不會同時觸發 tooltip 與抽屜——到現在仍然沒測過。**
   前一棒就標記了，我把可點範圍擴到 `/u/`，**仍然沒測**。有 CDP，用 375px
   觸控模擬實際點一次。
2. **production build 的顏色沒有量過。** 踩雷 #99 要求顏色只能在 production
   產物上驗，我全程只在 dev 量。新加的 custom property
   （`--bar-*` / `--spend-*` / `ChartLegendSwatch` 的 `--swatch-*`）走的是
   `dark:[background-color:var(…)]`，那**是**一個 utility，理論上不會被
   tree-shake——**但這是推論，不是量測**。收工前應該
   `pnpm build && PORT=3100 node --env-file=.env .output/server/index.mjs` 對 :3100 量一次。
3. **`/u/` 的 `ensureAllRecords()` 分頁迴圈從未在真實的 >200 筆資料上跑過。**
   David 只有 174 筆，永遠走「一頁就載完」那條。多頁那條路徑只有邏輯上是對的。
4. **`spend` 的「0 且涵蓋不完整」分支沒有真實資料走到**（David 沒有那種年份），
   只有單元測試蓋住。
5. **「路人 ＋ `show_cost = true`」只在 David 的帳號上驗過**，沒有第二個帳號。
   驗法是暫時翻他的 `show_cost`（`finally` 還原＋複查），不是建測試帳號。
6. **`/u/` 的圖表對「本人」呈現的是匿名視角那一份**（因為端點固定用 anon client）。
   那是刻意的——這一頁要回答「我分享出去別人看到什麼」——**但它是一個判斷，
   值得覆核**。如果 David 期待在自己的 `/u/` 看到完整的自己，這裡要改。
7. **`monthly_series`（156 個月的時間序列）仍然沒有任何呼叫端。** 那是刻意的
   （主 session 裁決：不做 156 個月的走勢），但那個欄位還在 RPC 裡回。

**以下 8–13 是 2026-09-07 那一輪（熱點圖總和／「其他」可展開／多刷抽屜／
`/app` 每年花費）新增的。** 每一條都是那一輪**沒有能力驗掉**的，
而它們看起來都很像已經驗過了。

8. ~~**★★ 那一輪所有的 UI 改動，一次都沒有在瀏覽器裡看過。**~~
   **2026-09-20 主 session 補測完畢**（Playwright ＋ 系統 Chrome，打正式站
   `https://filmnote-phi.vercel.app/u/clipwww`，1280 與 375 各一輪）。原文保留在下面
   當紀錄，因為**它的判斷是對的**——真的點下去才抓到兩件靜態檢查看不到的事。

   **通過的（三層都對：按鈕上的數字＝抽屜標題的數字＝實際列出的卡片數）**

   | 量到的 | 1280 | 375 |
   |---|---|---|
   | 分布長條三條 band（去了哪裡 5／版本 10）| 15／15 | 15／15 |
   | 多刷排行（上一輪做的，當對照組）| 19／19 | 19／19 |
   | 月份標籤 | **12／12** | **6／12**（見 §4-14）|
   | console error ／ pageerror | 0 | 0 |
   | 橫向溢出 | 無（1280=1280）| 無（375=375）|

   ★ **12 個月份抽屜的卡片數加起來正好 174**，等於他的公開紀錄總數
   ⇒ 那 12 個抽屜**不重不漏地切分了整份資料**，不是各自碰巧對。這比逐格比對強：
   逐格對可能同時漏同一批紀錄，總和對不上就藏不住。

   ⚠️ **原文說「下一棒的第一件事」是對的，但它低估了一件事**：真正抓到問題的不是
   「看一眼」，是**用能分辨的方式量**。我第一版腳本量出 19 個假紅燈（regex 只認
   「場」不認「次」）與 11 個假紅燈（canvas 位置只在迴圈前量一次，中間開關 11 次
   抽屜就失效了）——**兩批假紅燈都長得跟真 bug 一模一樣。** 詳見 §5 新增的三條。
9. ~~**軸標籤的點擊全程沒有在瀏覽器裡點過。**~~ **2026-09-20 點過了**：1280 下
   12 個月份標籤全部打得到，落點全在 `CANVAS` 上、y 偏移 14px（＝ grid 的
   `bottom: 28` 之下半格），x 與 `grid.left=34` ／ `right=12` ＋ `boundaryGap:false`
   的等距推算一致。**原文底下那條反向斷言沒有用到**——12 個月各自開出正確的月份、
   卡片數也各自對上，已經證明打得到的是軸標籤而不是別的東西。原文保留：
   已從 ECharts 6.1.0 的原始碼證實事件
   負載長什麼樣（`componentType` / `targetType: 'axisLabel'` / category 軸的
   `dataIndex`），也證實 `tooltip: { show: false }` 擋得住泡泡，
   **但沒有證明手指或滑鼠真的打得到那個文字元素。**
   ⇒ 反向斷言留在這裡：把 `onPick()` 裡 `componentType === 'yAxis'` 那一支改成走
   xAxis 的分支，點 **10:00 那一列**的總和**應該**開出「**週二**」的抽屜、
   列出來的卡整批換掉（10:00 是第 1 列，走 xAxis 就變成 `dataIndex + 1 = 2`；
   前提是軸從 09:00 起算，David 沒有 03–08 時的紀錄所以成立）。
   **若改壞了畫面上的東西沒變，代表根本沒點到軸標籤，整條互動是假的。**
10. **觸控裝置上點軸標籤會不會同時觸發 tooltip 與抽屜——未測。**
    上面第 1 條那份未驗清單現在多了「軸標籤」這個觸發面，
    而軸標籤比格子小得多，**只會更重要**。
11. **⚠️ `/u/` 完全沒有 `#table` 資料表 fallback（`grep` 零命中）
    ⇒ 兩條總和在 `/u/` 只存在於 canvas 裡。**
    `ChartBand.vue` 的檔頭把資料表寫成硬要求（「ECharts 的 heatmap 不支援 decal，
    色盲友善完全靠單色相明度階，而螢幕閱讀器從 canvas 什麼都拿不到」），
    所以這是**現行規範的違反**，不只是「缺口」——而這一輪把違反的面積擴大了
    （多了兩軌只有滑鼠碰得到的資料）。**2026-09-07 裁決是這一輪不補，但要記在案。**
    ⇒ 補的時候是把 `/app` 已有的 `#table` 一併搬過去，不是只補熱點圖那一張。
    **2026-09-14 追記：面積又擴大了一次**——月度趨勢新增 12 個可點的月份標籤，一樣
    只有滑鼠／觸控碰得到。復查仍是 `/app` 三處 `table-summary`+`#table`、`/u/` 零處。
    這個缺口現在也寫在 `SCREENS §9.2` 的引言框裡（那一節原本斷言「無障礙路徑仍然是
    `ChartBand` 的資料表」，那句話在 `/u/` 是假的）。**補不補是 David 的裁決。**
    ⚠️ 分布長條不在此列：那三張是 HTML 按鈕，tab 走得到。
12. **三位數總和的右緣沒有在瀏覽器裡量過像素。** SSR SVG 量到的是：
    `grid.right = 36` 時右側文字的原點 x = 283（`text-anchor="start"`，
    ＝ 311 − 36 + 標籤預設 margin 8），扣掉 12px 數字**約 0.6em 的估算前進寬**，
    三位數的右緣落在 303–305 / canvas 311。**那個字寬是估的，不是量的**，
    字型差異還在。⚠️ David 目前最大只有 60（兩位數），所以在他的帳號上把
    `right` 改回 30 看起來**完全正常**——**這個溢出只有重度使用者會遇到，
    「看起來太空」不是把它改回去的理由。**
13. **本人視角下 `spend_is_partial` 一樣會發生。** 這一條容易被讀反：
    「以上」不是只有路人才看得到。實測 David 174 筆裡 169 筆有票價，
    缺的 5 筆全部落在同一天有多筆紀錄的日子（連映拆分，票價記在第一筆）
    ⇒ **2020 與 2024 在他自己的儀表板上就是「以上」**。
    所以 `/app` 那條 band 的但書文案（「有幾筆沒有記票價」）不是備用分支，
    是**現在就看得到的常態**；它跟 `/u/` 路人版（「有未公開的票價」）
    **文案不可共用**，改一邊的時候另一邊不會跟著紅。

---

14. **★★ 375px 下 12 個月份標籤只畫得出 6 個，另外 6 個入口不存在**（§7 #250）。
    實測（2026-09-20，正式站、375 寬）：整條 309px 的軸**每 4px 點一次**，
    點得到的只有 **1／3／5／7／9／11 月**，等距 48px；偶數月在**任何位置**都點不到。
    截圖直接看得到 x 軸只印出那六個。

    成因：`MonthlyTrend` 的 `axisLabel` **沒有設 `interval`**，ECharts 預設是
    `'auto'`——放不下就自動隱藏一部分。375 下圖寬 309px、扣掉 `grid.left 34`
    ＋ `right 12` 只剩 263px，12 個「10月」這種三字標籤塞不下 ⇒ 丟掉一半。

    ⚠️ **這一條的諷刺之處值得記**：`MonthlyTrend.vue:87` 白紙黑字寫著
    「**月份標籤要可點，這不是加碼是必要條件**」，理由是
    「實線的 `symbolSize` 只有 6px，在觸控裝置上 6px 的目標基本上點不到」。
    **標籤正是為了手機而做的，而它在手機上少了一半。**
    1280 下 12 個全在，所以任何在桌機上做的驗收都會一路綠。

    ⇒ 修法是產品決定，不是技術決定，**三條路各有代價**：
    ① `interval: 0` 強制全印（會擠在一起，中文標籤會重疊）；
    ② 標籤改成只印數字「1…12」（省一半寬度，但失去量詞，`DS` 另有規定要查）；
    ③ 接受 375 只有 6 個入口，改讓資料點本身也可點（回到 6px 目標的老問題）。
    **不要在沒有裁決之前隨手改成 `interval: 0`** ——那會換到一個重疊的軸。

15. **`pick()` 的「最後點的贏」不變量在同一個 tick 內不成立**（§7 #251）。
    實測（2026-09-20，375、全新 context）：兩顆按鈕在**同一個 tick** 各 `click()`
    一次，四種情境的結果是

    | 情境 | 結果 |
    |---|---|
    | 只點 B | B ✅ |
    | A → 等 1.5 秒 → B | B ✅ |
    | A、B 同一個 tick | **A** ❌ |
    | B、A 同一個 tick | **B** ❌ |

    後兩列對稱 ⇒ **先點的贏**，不是隨機。兩個 handler 都確實被呼叫到
    （加了 capture 監聽器記錄，`['A','B']`），抽屜在 6 秒的輪詢裡**從未顯示過 B**。

    成因**不是** `9008e1e` 修掉的那一條（「進行中就早退」），是它**沒有涵蓋到的另一條**：

    ```js
    async function ensureAllRecords() {
      if (loadedAll.value) return        // ← 早退，1 個 microtask
      if (!inflight) { inflight = (async () => { … })().finally(…) }
      await inflight                     // ← 2 個 microtask
    }
    ```

    紀錄數 ≤ 200 時 `while (acc.length < total)` 一次都不跑 ⇒ 那個 async IIFE
    **完全同步跑完**、在回傳前就把 `loadedAll` 設成 true ⇒ 第二次呼叫走早退（快），
    第一次還卡在 `await inflight`（慢）⇒ **B 先寫、A 後寫，最後停在 A**。
    程式自己的註解寫著「所有呼叫端必須 await **同一個** promise，不可以早退」，
    而 `if (loadedAll.value) return` 就是一個早退。

    ⚠️ **嚴重性要講準：真人點不出來。** 這個窗口只有一個 tick（microtask 等級），
    兩次真實觸控相隔至少幾十毫秒、跨了很多個 tick，第一次的 `picked` 早就寫完了。
    **>200 筆的使用者反而是對的**（那時 IIFE 真的會 await，兩個呼叫端排在同一個
    promise 上、依序解析）。所以這是**不變量被違反**，不是使用者會遇到的症狀。
    ⇒ 要修的話最小的改法是在 `pick()` 裡帶一個序號、只有仍是最新的那次才寫
    `picked`；**不要把 `loadedAll` 那條早退拿掉**，那會讓每次點擊都多跑一輪。

---

## 5. 檢查機制：這一輪學到的（比程式本身重要）

`backend.md` 的歸納「**這個專案的錯幾乎都是檢查機制本身失效**」在這一輪
又發生了**四次，其中三次是我自己剛寫的檢查**：

1. **註解餵飽了字串比對**（#166）。`src.includes('monthlyBaselineSeries')`
   在把呼叫整個拿掉之後**照樣綠**，因為那一頁的註解裡就寫著那個名字，
   而且**註解寫得越詳細越容易中**。
2. **測試資料走不到它要測的分支**（#175）。為「聚合必須是多數」寫的第一版
   測試只給 10 筆，走的是 `INSIGHT_MIN` 的早退路徑，弄壞實作照樣綠。
   ⇒ **凡是測試某個分支，先確認資料真的走得到那個分支。**
3. **四個檢查全綠而頁面整個壞掉**（#173）。把一個匯出從 `composables/` 搬到
   `utils/` 之後 dev server 的模組圖沒更新，`/u/` 一條 band 都畫不出來，
   而 typecheck（讀 `.nuxt/imports.d.ts`，那份已經是對的）、lint、test、
   verify:all（`u/html-has-content` 檢查的是 **SSR 的 HTML**，而爆掉的是 client）
   **全部是綠的**。⇒ `scripts/check-hydration.mjs` 已擴充成
   「hydration mismatch ＋**任何** console error／pageerror 都算失敗」。
4. **探針本身抓錯元素**。`li.querySelector('div div')` 的**祖先比對會跑到 `li`
   外面去**（`:scope` 的經典陷阱），所以量到的是軌道不是填色，
   於是「免費那一列的條寬」報成滿寬 830px。用 `:scope > div + div > div` 才對。
   ⇒ **量出來的數字很奇怪時，先懷疑量法。**

**方法本身很簡單，而且四次都有效：寫完斷言就故意把它守的東西弄壞一次，
看是不是那一條變紅。**

### 2026-09-20 補：瀏覽器驗收那一輪，檢查機制又失效三次（全是我自己的腳本）

前面那四次是「程式綠而東西壞」。這三次反過來——**東西是好的，而我的量法報紅**，
而假紅燈在報告上跟真 bug 長得一模一樣：

1. **量詞只認「場」不認「次」。** 多刷排行那 19 列寫的是「N 次」，我的
   `/(\d+) 場$/` 抓不到 ⇒ 19 個假紅燈。**寫 regex 之前先去看畫面上實際的字。**
2. **canvas 的座標只在迴圈開始前量一次。** 中間開關了 11 次抽屜（抽屜鎖捲動、
   關掉還原），座標早就失效 ⇒ 2–12 月的點擊全打到別的 band 上，讀到的是**上一個
   抽屜的舊標題**——而那個標題看起來完全合理，是比對月份才發現不對。
   ⇒ **每次點擊前重新量，並用 `elementFromPoint` 確認腳下踩的是那張 canvas。**
   ⚠️ 探針本身也有坑：對視窗外的座標回 `null`，「被遮蔽」與「不在視窗裡」給出
   同一個答案（踩雷 #242）⇒ 先確認點在 viewport 內才採信。
3. **`pgrep -f "verify-drawers"` 會匹配到自己的輪詢 shell**（命令列裡就含那個字串）
   ⇒「還在跑」永遠為真，白等了三輪。用 `ps -eo command | grep "[n]ode .*verify-drawers"`。

⚠️ 另一條不是失效但值得記：**第一版的 `readDrawer()` 會把空抽屜當成穩定態**
（`prev` 初值 -1，第一次讀到 0、第二次 0===0 就回傳）。這一輪沒撞到，是因為
`/u/` 第一顆按鈕就把 174 筆全載完了。改成「非零且連續兩次相同」才安全——
否則它會在 `ensureAllRecords()` 還在飛的時候回報一個空抽屜，**正是踩雷 #169 的原型**。

### 這一輪新增的檢查

- `verify-all.ts` 的 `frontend/monthly-baseline` ×3（跨層對帳＋分辨力＋接線）
- `scripts/verify-u-public.ts` ×12（`/u/` 匿名驗收，已併進 `verify:all`）
- `scripts/verify-spend-visibility.ts` ×13（三種觀看者；**刻意不併進
  `verify:all`**，因為它會暫時翻動真實的隱私旗標，同 `verify-account-delete.ts`。
  主 session 已在 `verify:all` 收尾印出手動腳本清單，所以它不會被忘記）
- `scripts/check-hydration.mjs`（SSR 頁的瀏覽器端健康檢查）
- `scripts/screenshot-pages.mjs` 現在一律印 `html.class` 與 body 底色

---

## 6. 環境（在前一棒的基礎上補）

- **dev server 只聽 IPv6。** `lsof` 顯示 `[::1]:3000`，所以
  `curl 127.0.0.1:3000` 是 exit 7，**要用 `localhost:3000`**。
- **⚠️ 量測報告裡的「我切到暗色了」不是證據**（§7 #170）。records 實測到
  nuxt-color-mode 的**已存偏好會蓋掉 Emulation 的 `prefers-color-scheme`**。
  走 `$colorMode.preference` 的腳本不受那條影響，**但沒有印出證據就是推論**。
  ⇒ 任何提到亮／暗的數字都要附 `documentElement.className` 與
  `getComputedStyle(body).backgroundColor`，跟 `DS §7` 的規定值比對
  （亮 `rgb(248,237,220)`／暗 `rgb(29,22,16)`）；切換後**輪詢等 class 真的變了**
  再量，不要 sleep 猜。**開頭記下原本的 preference，收工還原**——
  多條 session 共用同一顆瀏覽器，誰切主題誰就污染對方。
- **要用「路人」的眼睛看同一頁：`browser.newContext()`**（§7 #172）。
  CDP 附著之後它是可用的，新 context 沒有 session。
  **不要清 cookie／localStorage，那會把 David 登出。** 用完 `ctx.close()`，
  **仍然絕不 `browser.close()`**。
- **暫時翻動真實資料的旗標時**：先讀出原值再還原（硬寫 false 會把本來就開著的
  帳號關掉）、還原放 `finally`、**結束前再查一次確認**。實測一次 SQL 打錯欄位名
  整支炸掉，`finally` 仍然正確還原了——那正是它存在的理由。
- **`IMPORT_TARGET_EMAIL`** 是「David 的帳號」的正確指定方式。
  ⚠️ 不要用 `limit 1`／「DB 裡唯一一筆 profile」，也**不要把 email 硬編進版控**。
- **`pnpm run lint` 拿到紅燈先看檔名是不是自己的**（§7 #167）。
  開工前我拿到 98 個錯，兩分鐘後同一個指令 exit 0——是別條 session 正在寫。
  `verify:all` 只對 `supabase/migrations`／`scripts` 的髒工作樹吵，
  **`app/**` 沒有任何警告**。
- **⚠️ commit 只 `git add` 自己動的檔案路徑。** 我這一輪的前兩個 commit 把
  records 的踩雷條目一起帶走了（內容沒掉，但 `git blame` 會指錯人）。
  共用檔：`docs/BUILD_PLAN.md`（兩條線都在追加，commit 前先看 diff）、
  `docs/design/SCREENS.md`（**主 session 的，不要 add**）、
  `nuxt.config.ts`、`package.json`。

---

## 7. `BUILD_PLAN §7` 號段

**charts 的號段是 `#165–#184`，已用到 `#175`**（165–175 連續，無跳號）。
**還剩 #176–#184，九個。** 用完跟主 session 要下一段，不要為了連號重排。

| # | 一句話 |
|---|---|
| 165 | DB 把欄位釘得再牢也證明不了前端有在用它——跨層的欄位要有跨層的斷言 |
| 166 | 字串比對的斷言會被**註解**餵飽 |
| 167 | 別的 session 在寫時，lint 會給你一份不是你造成的紅燈 |
| 168 | 踩雷 #88 會在「元件從 `/app` 搬到 SSR 頁」時復發，**而且只有暗色會紅** |
| 169 | 抽屜「開得起來」不等於「列得出東西」，空抽屜會理直氣壯地說謊 |
| 170 | 量測報告裡的「我切到暗色了」不是證據 |
| 171 | 「金額是 0 就不用畫」會把一個真實的事實整列吃掉 |
| 172 | 驗「因觀看者而異」用 `newContext()`，不要清 cookie |
| 173 | 四個檢查全綠而頁面在瀏覽器裡整頁不 render |
| 174 | 「最」宣稱唯一性，所以判準也必須是唯一性，不是大小 |
| 175 | 聚合光有 lift 不夠，它必須真的在講多數 |
