# ── TMDB（非商業階段用免費 key；一旦收費須升級商業訂閱）──────────────
TMDB_API_KEY=
NUXT_TMDB_API_KEY=

# ── Supabase ─────────────────────────────────────────────────────────
SUPABASE_URL=
SUPABASE_KEY=                 # sb_publishable_… 可公開曝露
SUPABASE_SECRET_KEY=          # sb_secret_… ★ 僅 server 端，絕不可加 NUXT_PUBLIC_
SUPABASE_PROJECT_ID=          # 只給 npm run db:types
DATABASE_URL=                 # postgresql://… ★ 只給 npm run db:sql 套 migration 用，含 DB 密碼

# ── 應用程式 ─────────────────────────────────────────────────────────
NUXT_PUBLIC_SITE_URL=http://localhost:3000
# COPYRIGHT_CONTACT_EMAIL 已於 2026-09-07 移除（David 裁定）：本服務不公告電子郵件
# 受理窗口，§90-4 第 3 款要公告的窗口就是 /legal/dmca 那張表。不要加回來。

# 排程端點（/api/cron/tmdb-refresh、/api/cron/tmdb-purge）的共用密鑰。
# 程式兩個名字都收，但兩者不是同義詞：
#   NUXT_CRON_SECRET — 本機開發用，Nuxt runtimeConfig 的命名慣例
#   CRON_SECRET      — ★ 部署到 Vercel 時**必須**是這個名字。Vercel 只有在
#                      專案環境變數叫 CRON_SECRET 時，才會在觸發 cron 時自動帶上
#                      Authorization: Bearer <值>。改成別的名字排程照跑，
#                      但請求不帶 header，端點一律 401——而 cron 面板顯示成功。
NUXT_CRON_SECRET=
CRON_SECRET=

# ── 舊 log 匯入（scripts/import-mylog.ts）─────────────────────────────
# 要把觀影紀錄匯給哪一位使用者，以 auth.users 的 email 精確指定。
# 刻意不提供預設值也不寫死在程式碼裡：DB 內有多個 session 建的測試帳號，
# 用「唯一一筆 profile」之類的假設猜錯，會把紀錄匯到別人身上。
# 也可以用 CLI 參數傳：pnpm import:mylog -- --email <address> --apply
#
# ⚠️ 2026-09-20 起這個變數有**兩個意思**（David 指定沿用同一個變數）：
#   ① CLI 匯入腳本要把紀錄寫給誰（原本的意思）
#   ② 誰可以使用 /app/import 這個「匯入舊紀錄」UI（server/utils/import-auth.ts）
# 也就是說，為了「把紀錄匯給另一個帳號」而改這個值，會**同時**把匯入 UI 的
# 使用權交給那個帳號——那通常不是改它的人想做的事。
# ★ 沒設值時是**全部拒絕**（失敗方向關閉），不是全部放行。
# ★ 應用程式端讀的就是這個名字（不是 NUXT_ 開頭的），Vercel 上也要設成這個名字。
IMPORT_TARGET_EMAIL=

# #333 常駐的非 staff 測試帳號（zz 前綴）。用來驗 RLS 對一般使用者的行為；不要拿它做會留資料的事。
TEST_NONSTAFF_EMAIL=
TEST_NONSTAFF_PASSWORD=
